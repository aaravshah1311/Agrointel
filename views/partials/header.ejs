<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AgroIntel | <%= title %></title>
  <style>
    .navbar {
  border-bottom: 2px solid transparent;
  border-image: linear-gradient(to right, #0d6efd, #4dabf7);
  border-image-slice: 1;
}

  </style>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
  
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container">
      <!-- Brand -->
      <a class="navbar-brand fw-bold text-success" href="/">
        <i class="fas fa-leaf me-2"></i> AgroIntel
      </a>

      <!-- Toggler -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Links -->
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
          <% if (user) { %>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="/user/dashboard">Dashboard</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="/user/select-crop">Change Crop</a>
            </li>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="/user/check-land">Check My Land</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-danger px-3 fw-semibold" href="/auth/logout">
                <i class="fas fa-sign-out-alt me-1"></i> Logout
              </a>
            </li>
          <% } else { %>
            <li class="nav-item">
              <a class="nav-link fw-semibold" href="/auth/login">Login</a>
            </li>
            <li class="nav-item">
              <a class="btn btn-success px-3 fw-semibold" href="/auth/signup">Sign Up</a>
            </li>
          <% } %>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Alerts -->
  <div class="container mt-4">
    <% if (success_msg && success_msg.length > 0) { %>
      <div class="alert alert-success" role="alert"><%= success_msg %></div>
    <% } %>
    <% if (error_msg && error_msg.length > 0) { %>
      <div class="alert alert-danger" role="alert"><%= error_msg %></div>
    <% } %>
  </div>
